php-mogilefs 0.7.5 beta3:
	* New API: MogileFs::isConnected() and mogilefs_is_connected()
	* Fixed segfault in MogileFs::put() if client was not connected before
	* Various new tests added
	* More robust MogileFs::rename()-test
	* Removed procedural API to make testing easier
	* Added debugging mode. Compile with "-DMOGILEFS_DEBUG"
	* Adding static method MogileFs::isInDebuggingMode()
	* Cleaned up request handling

php-mogilefs 0.7.5 beta2:
	* Fixed file handle leak because of missing ne_session_destroy() in
	MogileFs::put()
